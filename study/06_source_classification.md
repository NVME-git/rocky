# 06 · Source Classification — AI-Authored vs Own Code

**Source files:** `src/db.rs` · `src/main.rs` — `run_diff()`, `run_backfill()`

---

## The core problem

```mermaid
flowchart LR
    subgraph COMMIT["A commit in your name"]
        A["git log shows:\nAuthor: You <you@email.com>"]
        B["But did YOU write it?\nor did Claude?"]
    end

    A --> B

    B --> OWN["own_code\nYou understand it"]
    B --> AI["ai_prompt\nYou directed it\nbut didn't write it"]

    OWN --> ROCKY_ASSUMES["Rocky assumes: known\nstability = 2.0"]
    AI --> ROCKY_PROBLEM["Rocky problem: assumed known\nbut NOT verified"]
```

---

## Available signals — reliability ranking

```mermaid
flowchart TD
    subgraph HIGH["✅ Reliable signals"]
        S1["Co-Authored-By trailer\nin git commit body\n─────────────────\ngit log --format=%B\nContains: Co-Authored-By: Claude"]
        S2["Quiz history\nclean_pass_count vs scaffolded_pass_count\n─────────────────\nRocky controls this — ground truth\nIndependent of code origin"]
    end

    subgraph MED["⚠️ Moderate signals"]
        S3["Commit message patterns\n'Co-authored with Claude'\n'Generated by AI'\n─────────────────\nConvention-dependent\nNot enforced by tooling"]
        S4[".claude/settings.json\nhooks present in repo\n─────────────────\nIndicates Claude Code active\nbut not per-commit"]
    end

    subgraph LOW["❌ Unreliable signals"]
        S5["Commit author name\ngit config user.name\n─────────────────\nAlways shows human name\neven for AI-written code"]
        S6["GitHub PR view\n'Copilot' badge or merge commit\n─────────────────\nOnly visible in UI\nnot in git log"]
    end

    HIGH --> TRACK1["Track 1: co_authored flag\nset at diff/backfill time"]
    MED  --> TRACK1
    HIGH --> TRACK2["Track 2: independence score\nbuilt from quiz history"]
```

---

## Co-Authored-By detection — how it works

```mermaid
flowchart LR
    GIT_LOG["git log --format=%B SHA"]
    BODY["commit body text"]
    GREP["scan for:\nCo-Authored-By: Claude\nCo-authored-with: claude\nco-authored-by: GitHub Copilot"]

    GIT_LOG --> BODY --> GREP

    GREP --> FOUND{"match\nfound?"}

    FOUND -- YES --> FLAG_TRUE["co_authored = true\nstability = 1.5 at insert\ndifficulty = 0.4"]
    FOUND -- NO  --> FLAG_FALSE["co_authored = false\nstability = 2.0 at insert\ndifficulty = 0.3"]

    FLAG_TRUE  --> DB["nodes table"]
    FLAG_FALSE --> DB
```

---

## Proposed two-track approach

```mermaid
flowchart TD
    subgraph TRACK1["Track 1 — Origin tag (best-effort)"]
        T1A["co_authored: bool\nset from Co-Authored-By at diff/backfill"]
        T1B["source: TEXT\n'own_code' | 'ai_prompt' | 'task_prompt'"]
        T1C["Affects:\ninitial stability\ninitial difficulty"]
    end

    subgraph TRACK2["Track 2 — Independence score (authoritative)"]
        T2A["clean_pass_count: int\nQ&A passed without clue/explain/simpler"]
        T2B["scaffolded_pass_count: int\nQ&A passed with clue or explain used"]
        T2C["independence_score = clean / (clean + scaffolded)"]
        T2D["Affects:\nfuture stability update weight\nhard mode unlock threshold"]
    end

    T1A --> T1C
    T1B --> T1C
    T2A --> T2C
    T2B --> T2C
    T2C --> T2D

    T1C --> COMBINED["Combined signal:\norigin + quiz behaviour\n= full picture of understanding"]
    T2D --> COMBINED
```

---

## Source field values

| Value | When assigned | Example |
|---|---|---|
| `own_code` | `rocky diff` — no Co-Authored-By found | You wrote the function yourself |
| `ai_prompt` | `rocky diff` or `backfill` — Co-Authored-By found | Claude wrote it, you reviewed/merged |
| `task_prompt` | `rocky "task desc"` — user described intent | You described what to build, not what exists |

---

## Effect on initial stability

```mermaid
flowchart LR
    subgraph CURRENT["Current (all same)"]
        C1["any diff topic\nstability = 2.0\ndifficulty = 0.3"]
    end

    subgraph PROPOSED["Proposed"]
        P1["own_code\nstability = 2.0\ndifficulty = 0.3"]
        P2["ai_prompt\nstability = 1.5\ndifficulty = 0.4\n─────────\nresurfaces ~33% sooner"]
        P3["task_prompt\nstability = 2.0\ndifficulty = 0.3\n─────────\nneutral — user described intent"]
    end

    CURRENT --> PROPOSED
```

---

## Independence score — quiz feedback loop

```mermaid
flowchart TD
    QUIZ_START["Q&A session begins"]

    QUIZ_START --> Q["question shown\n(canonical or generated)"]

    Q --> CLEAN{Answered without\nclue, explain, simpler?}

    CLEAN -- YES --> CLEAN_INC["clean_pass_count += 1"]
    CLEAN -- NO  --> SCAFF_INC["scaffolded_pass_count += 1"]

    CLEAN_INC --> SCORE["independence_score = clean / (clean + scaffolded)"]
    SCAFF_INC --> SCORE

    SCORE --> THRESHOLD{score ≥ 0.8?}

    THRESHOLD -- YES --> HARD_UNLOCK["hard mode available\nrocky quiz --hard\nhigher stability reward"]
    THRESHOLD -- NO  --> NORMAL["normal difficulty\nstandard stability update"]
```

---

## Planned DB columns

```sql
-- Track 1: origin
ALTER TABLE nodes ADD COLUMN co_authored INTEGER NOT NULL DEFAULT 0;
ALTER TABLE nodes ADD COLUMN source TEXT NOT NULL DEFAULT 'own_code';

-- Track 2: independence
ALTER TABLE reviews ADD COLUMN clue_used INTEGER NOT NULL DEFAULT 0;
ALTER TABLE reviews ADD COLUMN explain_used INTEGER NOT NULL DEFAULT 0;
ALTER TABLE reviews ADD COLUMN simpler_used INTEGER NOT NULL DEFAULT 0;
ALTER TABLE reviews ADD COLUMN followup_count INTEGER NOT NULL DEFAULT 0;
```

---

## 📝 Annotation space

> Add your notes here. Questions to consider:
>
> - Is `stability = 1.5` for `co_authored` aggressive enough? Too aggressive?
> - Should `task_prompt` topics also get lower stability since the user hasn't verified they can build it?
> - Should `independence_score` decay over time, or only update with new quiz data?
> - Should `co_authored` affect stability updates too, not just initial stability?
> - Is there a case for `rocky diff --mark-own` to manually override AI detection on a commit?
> - What happens when a topic starts as `ai_prompt` but the user later demonstrates mastery?
>   Should the `source` tag be immutable, or should it flip to `own_code` after sufficient clean passes?
